PORT=8080
MONGODB_URI=mongodb+srv://<user>:<pass>@cluster0.xyz.mongodb.net/mydb?retryWrites=true&w=majority

# simple shared secret your PHP form includes as a header X-API-KEY
API_KEY=supersecretkey

# SMTP (use your provider or Gmail App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASS=your_app_password
FROM_EMAIL="Loop & Logic <info@loopandlogic.dev>"

# Zoom Server-to-Server OAuth
ZOOM_ACCOUNT_ID=your_zoom_account_id
ZOOM_CLIENT_ID=your_zoom_client_id
ZOOM_CLIENT_SECRET=your_zoom_client_secret
